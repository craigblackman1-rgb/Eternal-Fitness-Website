# Work Order: Eternal Fitness — Client Data Consolidation & Document-Led Client Portal — 2026-07-20

OWNER: Claude Code — **ACTIVE, signed off by Craig 2026-07-20T12:02Z. Lane F added 2026-07-20T19:22Z, same session continuation, Craig-directed. Lanes I/J/K added 2026-07-22, Craig-directed (paper document storage, conversion-tool backlog, portal auth rework).**
SCOPE: `eternal-fitness-website` (`D:\apps\eternal-fitness-website` — hub client records, document engine, PAR-Q, a new Process & Quality module, and a new client-portal auth surface); Eternal Fitness production Postgres (VPS, `eternal_fitness` DB, reached via the Coolify SSH tunnel `127.0.0.1:5433` → `10.10.10.2:5432`, role `ef_app`); read-only reference to `decoded-ops-hub`'s `src/components/decoded-ops/operations/OperationsFramework.tsx` (pattern to port, not a file to edit — no write access to `decoded-ops-hub` under this Work Order). Registry: `infrastructure/.context/active-workorders.md`.

GOAL: Two phases, sequenced so Phase 1 makes the hub the actual single source of truth for every EF client before Phase 2 puts anything client-facing in front of them.

- **Phase 1** — Pull every client's record into the hub from wherever it currently lives (Trainerize, Outlook, OneDrive, paper), port Decoded Ops' Process & Quality System pattern into the EF hub and populate it with EF's own content, and write simple SOPs for Esther covering "migrate a client," "onboard a new client," and "build a plan in the hub."
- **Phase 2** — A documents-only client portal: client login, read-only view of what they've signed (T&Cs, Risk Assessment, Annual Review, PAR-Q once migrated), what's still outstanding, and a mirror of the update emails already being sent — reducing reliance on one-off emailed links. Explicitly **not** the full Trainerize-equivalent session-logging/plan-delivery build scoped in `EF_Trainerize_Accessibility_Scope_Jul2026.md` (2026-07-15) — that charter's Phase 1 (plan delivery + logging) and Phase 2 (accessibility-first UX) become **Phase 3** of this combined roadmap, pulled forward only if a real need (e.g. a VI client actually starts using the portal) makes it urgent.

MUST:
- Prod DB writes (any client record insert/update, any PAR-Q/document import) need Craig's **explicit per-session go-ahead** — established pattern from the PAR-Q work (`.context/decisions.log`, 2026-07-10), not just Work Order continuation.
- No data loss on the PAR-Q → document-engine migration — every existing `signed_parq` row must have a verified equivalent `client_documents` row before any legacy table is touched, per `charter-document-engine.md` Phase 4.
- Client documents/portal work builds on the existing `document_templates` + `client_documents` engine (snapshot-on-send, versioned, dual-signable) — no new one-off tables for a document type the engine already models.
- Process & Quality System: port `OperationsFramework.tsx`'s three-tab pattern (Process Register / SOPs / Improvement Log), but **write EF's own content** — do not copy TackleBag's or Decoded Ops' process entries. Esther/Craig supply the actual processes.
- Hard rules from the project `CLAUDE.md` apply throughout: no invented client stories, no fitness-marketing language on clinical content, verify studio equipment before any plan-related SOP references it, clinical populations get clinical framing.
- Any client-facing communication (portal invite, "your documents are ready" email, etc.) is generated → reviewed by Esther → sent, never auto-sent — matches the existing update-email decision (`state.md`/`decisions.log`).
- Accessibility baseline (WCAG 2.2 AA — keyboard operability, contrast, touch targets, no colour-only signalling) applies to the Phase 2 portal from the first screen, even though the deeper VI-specific work (voice logging, SR-tested plan read-order) stays in Phase 3 — see `EF_Trainerize_Accessibility_Scope_Jul2026.md` §3.1 for the exact baseline checklist.
- Log Trainerize export method used (manual CSV, screen-scrape, whatever's actually available) — there is no existing Trainerize *client*-data import (only an exercise-library scrape exists today), so this is new integration work, not a reuse of existing scripts.

DECIDE YOURSELF: exact structure/wording of the SOPs (as long as they follow the Process & Quality System's existing trigger/owner/steps/"what good looks like" shape); which client goes first in the Trainerize/Outlook/paper consolidation; whether to batch-import via a script (`scripts/`, matching the `import-parq.mjs` / `create-update-draft.mjs` pattern) or hand-enter for low-volume sources; Process Register content structure/categories; portal UI layout within the existing hub design system; which document types are already engine-native (T&Cs, Risk Assessment, Annual Review) vs still need the PAR-Q migration before they can appear on the portal.

ASK FIRST (`[GATE]`):
- Craig's sign-off to start this Work Order at all (per his instruction — this file is a draft for review in a later session).
- Any write to Eternal Fitness production Postgres — client record inserts/updates, PAR-Q migration writes, document-engine writes.
- Adding client authentication/login as a new surface (new attack surface + first time clients get any account).
- Sending anything to a real client (portal invite, document reminder, etc.) — draft and stage only.
- Deprecating/retiring the legacy `signed_parq` table or the bespoke `/agreement` form once the engine migration is proven — explicit go-ahead per `charter-document-engine.md`'s own Phase 4 note.
- Any decision on pulling Phase 3 (session logging / plan delivery / deep accessibility) forward — that's a scope-expansion call, not an execution detail.
- Any write to `client_documents`/`portal_accounts` under Lanes I/K — new columns, Sarah's scanned agreement, or the portal auth rework all touch prod Postgres or the live auth surface.
- Removing/replacing the passwordless magic-link portal auth with password + reset (Lane K) — a real security-surface change, confirm before it ships.

## DONE (ticks to zero = stop)
- [ ] Every client record consolidated into the hub `clients` table with a documented source (Trainerize / Outlook / OneDrive / paper) and no duplicate/orphaned entries
- [x] Trainerize client-data extraction method chosen and documented — Craig decided manual entry 2026-07-20, no script needed
- [ ] Process & Quality System (Process Register / SOPs / Improvement Log) ported into the EF hub and populated with EF-specific content, not placeholder/copied content — module + migration live, **content not yet written**
- [ ] Three SOPs written and reviewed by Esther: "migrate a client into the hub," "onboard a new client," "build a plan using the hub"
- [ ] PAR-Q fully migrated onto the `document_templates`/`client_documents` engine with 1:1 verified parity against existing `signed_parq` rows (`charter-document-engine.md` Phase 4) — script written, **not run, not verified against live records**
- [x] Client login/auth surface built, scoped to each client's own data only — **deployed live 2026-07-21** on staging.eternal-fitness.co.uk (fixed a real infinite-redirect-loop bug found during the deploy check — `/portal/login` was nested inside its own auth-gating layout; restructured into a `(protected)` route group matching the hub's working pattern). No real client account created yet.
- [x] Read-only client portal live: shows signed documents, outstanding/unsigned documents, and a history of update emails sent to that client — **deployed live 2026-07-21**, same fix as above.
- [x] Portal passes a WCAG 2.2 AA baseline check — **2026-07-21: real independent check** (not the earlier self-check) — computed actual contrast ratios from the live CSS tokens rather than assuming they'd pass. Found and fixed a genuine failure: 3 of 5 `StatusBadge` colour tokens (primary/warning/success) measured 2.5–4.2:1 against their tinted backgrounds, under the 4.5:1 AA text minimum — most visibly "Signed"/"Sent", the two labels a client sees most. Added darker `--status-*-text` badge-only variants. Focus ring itself already passed (3.1–3.4:1). Still not a full screen-reader/keyboard walkthrough in a real browser session — that remains outstanding before inviting a client.
- [ ] First real client invited to the portal and confirmed it works end-to-end (sign-in → see their own documents only)
- [x] Scanned/paper documents can be stored against a client's record as a copy of the original (`client_documents.source_type`/`source_file_name`/`source_file_mime`/`source_file_size` + `client_document_files`) — **DONE + DEPLOYED 2026-07-22.** Sarah Tyler's Personal Training Agreement is the first real case: `client_documents` id `a74a1ef7-0c19-478c-b5e2-538a9304e102`, `kind='terms'`, `status='signed'`, `source_type='scan'`, 183,462 bytes, verified byte-for-byte against the source PDF.
- [ ] Paper→digital conversion tool scoped (not built) — decide which output the plan-generation agent actually needs: HTML-rendered document body, structured client-profile fields, or both
- [ ] Portal auth rewritten from passwordless magic-link to password + reset — initial password auto-generated and emailed only when Esther adds/invites a client from the hub; no self-serve request-a-link/signup surface remains
- [ ] SMTP/SendGrid delivery backend confirmed live in prod (existing open item, now also the suspected cause of Craig's "no email" portal report — not a new bug, folded in here)
- [x] **New (2026-07-20, Craig-directed): the client document engine (consent/terms/risk-assessment/annual-review) visually matches the new brand design system** — masthead, eyebrow, serif title, meta strip, accessibility toolbar, sign-boxes, footer; deployed and verified live
- [x] Document email sending (primary = email to client, fallback = copy link, resend once sent) — built, deployed, a real bug caught and fixed before ship; not yet tested with a live send

## LANES (dependency graph)
- **Lane A — Client data consolidation** (`clients` table + source docs) — no dependencies, start first.
- **Lane B — Process & Quality System + SOPs** (new hub module + `.md`/content) — independent of A, can run in parallel.
- **Lane C — PAR-Q → document engine migration** (`document_templates`, `client_documents`, retire `signed_parq` path) — independent of A/B, but blocks part of Lane D (portal can't show PAR-Q status until this lands).
- **Lane D — Client portal MVP** (auth + read-only document/update view) — depends on Lane C for full document coverage; T&Cs/Risk Assessment/Annual Review are already engine-native so a partial Lane D (those three doc types + update-email mirror) could ship before Lane C finishes if Craig wants to sequence it that way.

A and B run in parallel from the start. C runs in parallel with A/B. D starts once auth is designed but can't be considered "done" until C lands (full document coverage) — flag this dependency explicitly rather than silently shipping a portal that's missing PAR-Q status.
- **Lane I — Scanned/paper document storage** (added 2026-07-22, Craig-directed) — independent of A–H, small schema + storage addition to the document engine. Blocks nothing; Sarah's Personal Training Agreement is the first real unit.
- **Lane J — Paper→digital conversion tool (backlog, not built)** (added 2026-07-22) — depends on Lane I landing first (needs `source_type='scan'` records to operate on). Scoping only this round.
- **Lane K — Portal auth rework: magic-link → password + reset** (added 2026-07-22, Craig-directed) — depends on Lane D's existing portal auth surface; replaces it rather than extending it. Independent of I/J.
- **Lane F — Full hub design-consistency sweep, remaining routes** (added 2026-07-20T19:22Z) — independent of A/B/C/D, pure UI restyle, no schema/data changes. Units are independent of each other (distinct files/routes) except where noted — safe to run several in parallel without worktrees, since no two units touch the same file. **Loop mechanics** (Craig-directed): originally OpenCode via CLI; the first 4-parallel `opencode run` launch stalled (36 min, near-zero CPU, no file changes — bootstrapped but never started agent work, likely contention from 4 concurrent instances against the same repo/session store) — killed 2026-07-20T~19:58Z. **Switched to Claude Code implementing directly via Haiku subagents** (Craig-directed, token-efficient model for mechanical restyle work) → Claude Code (Sonnet, this session) verifies (`tsc --noEmit`, `npm run build`, diff review, functionality-preserved check) → ticks DONE → dispatches the next unit. Repeats until Lane F is empty or only GATE remains. Push stays `[GATE]` every time per the standing hard rule, regardless of how many units have landed locally.

## UNITS

### Lane A — Client data consolidation
- [AUTO] Audit current `clients` table + all extension migrations (`20260509_training_app.sql`, `20260603_seed_clients.sql`, `20260630_client_profile_extensions.sql`, `20260704_client_master_consolidation.sql`) — produce a field-by-field map of what the hub already captures per client. VERIFY: map covers every column, flags any that are unused/dead.
- [AUTO] Determine the actual Trainerize export path available (CSV export, admin panel screen-scrape, or manual read-and-type) — there is no existing client-data script, only the exercise-library scraper (`scripts/scrape-trainerize-exercises.mjs`) which is a different data type. VERIFY: method documented with a worked example on one real client.
- [AUTO] ~~Build (or extend `import-parq.mjs`'s pattern into) a reusable client-import script if volume justifies it; otherwise document a manual entry SOP if only a handful of clients are on Trainerize/paper.~~ **Decided 2026-07-20 by Craig: manual entry.** No import script needed — client data will be typed into the hub by hand from Trainerize/Outlook/paper sources.
- [AUTO] Draft a JSON/CSV staging file per client from Outlook/OneDrive/paper sources, cross-checked against the field map for completeness. VERIFY: every listed client has a staging file, no client silently dropped.
- [GATE] Writing any staged client record to production Postgres.

### Lane B — Process & Quality System + SOPs
- [AUTO] Read `D:\apps\decoded-ops-hub\src\components\decoded-ops\operations\OperationsFramework.tsx` in full to confirm the exact tab structure (Process Register / SOPs / Improvement Log) and data shape (`ProcessEntry[]`, `SOP[]`, `ImprovementEntry[]`) before porting. VERIFY: structure matches what's described here — flag any drift.
- [AUTO] Port the component shell into the EF hub (new route, matching hub design system, not Decoded Ops' branding) with empty/placeholder arrays — no content yet. VERIFY: renders correctly inside the hub's existing nav/layout.
- [AUTO] Draft EF's actual Process Register entries (what processes exist today: client migration, onboarding, plan-building, PAR-Q handling, update-email sending) with owner/trigger/steps for each, in the same shape as the TSX arrays. VERIFY: every entry reviewed for accuracy against how the hub actually works today, not aspirational.
- [AUTO] Write the three required SOPs ("migrate a client," "onboard a new client," "build a plan in the hub") in the SOP shape (trigger/owner/steps/"what good looks like"). VERIFY: each SOP is followable by someone who hasn't done the task before — Esther is the real test reader.
- [AUTO] Decided 2026-07-20: DB-backed editor, not hardcoded — Esther needs to edit SOPs/process entries herself without a code change each time. Design a minimal schema (`process_entries`, `sops`, `improvement_log` tables or equivalent) and a simple hub-admin CRUD UI reusing existing hub form patterns. VERIFY: schema supports the same shape as `OperationsFramework.tsx`'s arrays; a non-technical edit (add/update one entry) requires no code deploy.

### Lane C — PAR-Q → document engine migration — **DONE 2026-07-21**
- [AUTO] Read `charter-document-engine.md` Phase 4 note in full and confirm current state of `signed_parq` vs `document_templates`/`client_documents` before starting. VERIFY: no assumptions carried over from the July 4 charter without re-checking against the live schema. **Done — re-verified live schema before this session's migration.**
- [AUTO] Build a `document_templates` entry for PAR-Q (structured `body` JSON, matching the 29-question structure already used) and a migration script to snapshot each existing `signed_parq` row into `client_documents`. VERIFY: 1:1 field mapping documented, spot-checked against at least 3 real client records. **Done — superseded the original raw-HTML-table body plan with the real interactive feedbackSections/feedbackConsents schema (same one built for the Feedback Questionnaire), a strictly better outcome: actual typed/radio-group fields a screen reader can operate, not a read-only dump. Spot-checked Colin Farley (real "yes" + free-text), Sarah Tyler, Craig Blackman — byte-for-byte match.**
- ~~[AUTO] Fix the known Colin-flow caveat (anon read of `/parq/edit/[id]` fails under authenticated-only RLS)~~ — **superseded, not done as originally scoped.** The document engine's public sign route (`/documents/[id]/sign`) already uses the service-role pattern for every kind; PAR-Q inherits that for free by moving onto the engine, rather than needing its own fix.
- [GATE] Running the migration against production Postgres. **Done 2026-07-21, Craig's explicit go-ahead — 17/17 rows, verified, zero legacy data touched.**
- [GATE] Retiring/deprecating the legacy `signed_parq` table or bespoke `/agreement` form once parity is proven. **Partially done, deliberately not completed:** stopped generating NEW links to `/parq`/`/parq/edit/[id]` (removed `SendDocumentLink.tsx` + the buttons using it) so no client is ever newly sent a standalone-page link again — but the table and pages themselves are NOT deleted (safety net for any outstanding pre-migration link, plus `tracker`/compliance-tab code still reads `signed_parq` directly and was deliberately left untouched — that's medical-clearance-critical code needing its own reviewed pass, not a side effect of this one).

### Lane C (cont.) — Agreement → document engine migration — **DONE 2026-07-21, same session**
Craig: "migrate that too." Found the `'terms'` document_templates kind was already the real, dual-signed
Personal Training Agreement (updated 2026-07-04) — never formally taken over from the standalone
`/agreement` page, which nothing in the hub even linked to. No new kind needed: relabelled
`DOCUMENT_KIND_LABEL.terms` → "Personal Training Agreement" and built
`scripts/migrate-agreements-to-engine.mjs` to backfill all 6 `signed_agreements` rows into
`client_documents`. Hit and fixed a real bug mid-run: 2 rows had `client_id = null` (never linked to a
`clients` row); the script now resolves by exact case-insensitive name match, skipping — never
guessing — on ambiguity. Re-ran clean: 6/6, 0 skipped, spot-checked. `DocumentRegister.tsx`,
`clients/[id]/page.tsx`, and `app/hub/(protected)/documents/page.tsx` all stopped reading
`signed_agreements` (the last one also stopped reading `signed_parq`) to prevent double-listing.
`/hub/agreements` relabelled "(legacy record)", not deleted. **Found and deliberately NOT fixed**: a
real, separate data-integrity bug — `signed_agreements`' package/payment/clinical columns are edited by
`AgreementDetailClient.tsx` while the live client page edits the same concepts on `clients` via a
totally separate, unsynced path. Spawned as its own background task, not folded into this migration.
**Behaviour change, not silently dropped**: new-flow agreements send as a sign-link email, not an
auto-attached PDF — a signed copy is still obtainable via the document engine's existing
"Print or save as PDF" toolbar button. Result: every document kind in the hub now shares one
send/resend mechanism; nothing generates a fresh standalone public-page link anymore.

### Lane D — Client portal MVP
- [AUTO] Design client auth (magic-link or password, scoped to own data only) — no implementation yet, just the approach + a WCAG 2.2 AA check on the login flow itself (no CAPTCHA, no puzzle 2FA per the existing accessibility charter §3.1). VERIFY: approach reviewed against baseline accessibility checklist before build starts. **Decided 2026-07-20 by Craig: magic-link, approved as designed in `.context/lane-d1-client-auth-design.md`.** Cleared to start the unit below.
- [AUTO] Build the read-only portal view: signed documents, outstanding/unsigned documents, update-email history — reusing existing hub components/data where possible rather than a parallel UI system. VERIFY: a client can only ever see their own records (tested with two different client accounts). **Built 2026-07-20** (`78a650b`) — magic-link auth + `/portal/*` view. Not yet run/deployed live, not yet tested with real accounts.
- [AUTO] Run the WCAG 2.2 AA baseline pass (keyboard-only walkthrough, contrast check, touch target sizing, no colour-only status indicators) on every portal screen before any client sees it. VERIFY: documented pass/fail per screen, not a single overall claim. **Doc produced** (`.context/lane-d2-wcag-check.md`) but this was a self-check during the build, not an independent walkthrough — worth a real pass before inviting anyone.
- [GATE] Implementing client login as a live auth surface on production.
- [GATE] Inviting any real client to use the portal.

### Lane E — Design system rollout + document delivery (added 2026-07-20, Craig-directed, not in the original scope)
- [AUTO] Port the brand-staging design system (`D:\apps\design-systems\brand-staging-2662e9`) into the client document engine (all 4 kinds: terms/risk_assessment/annual_review/consent) — masthead, eyebrow, serif title, meta strip, functional accessibility toolbar (text size + high contrast), numbered sections, sign-boxes, footer. **Done** (`0767276`), deployed and verified live on staging.
- [AUTO] Build the `consent` document type end-to-end (schema, interactive checkbox capture, sign flow, API persistence) using the accessible reader-controls variant from the reference (not the card/progress-bar alt). **Done** (`8b3b689`), migrations run against prod, template live, verified via a real staging document.
- [AUTO] Align hub client detail + edit pages to the reference mockups (tabs, meta chips, segmented controls, field contrast, sticky save bar) — scoped narrowly after confirming the sidebar and client list table already matched. **Done** (`0601531` + `c4586a4` safety fix for a shared-component blast-radius issue caught before push), deployed and verified live.
- [AUTO] Fix the document-page focus ring to match the hub's existing focus convention (was a 6px full-strength rose glow vs. the hub's 3px/30%-opacity rose ring). **Done** (`2fb225e`), deployed and verified live.
- [AUTO] Document email sending — primary method = email the client the sign link (reusing the existing branded email layer), copy-link kept as manual fallback, "Resend email" once already sent. **Done** (`06b8c6b`/`28d6fb2`/`d4ac915`) — caught and fixed a real bug before shipping: the email template built a `signUrl` variable but never put it in the HTML, so the "Review & sign" button it promised didn't exist. Deployed. Not yet tested with a real send (no email backend confirmed configured — dry-run fallback applies if not).
- [x] Broader hub design sweep — **client detail page done 2026-07-20** (later session): full 6-tab rollout of `hub-client-detail.html` (page header, tab strip, Profile restructure, Training table) plus a button-radius restyle across Compliance/Training/Plan Agent/Updates (`rounded-full` → `rounded-lg`) so all six tabs read consistently. Commits `2acaf4e`, `211f3f7`, both live on staging. Still open: `PackagePaymentsCard.tsx` has the same pill-button pattern, not yet fixed; sweep beyond the client detail page (e.g. `/hub/clients` list, other hub routes) not covered.
- [x] Six new hub screen mockups (dashboard, exercise library, process & quality, reports & updates, SOP detail, studio equipment) restyled in code, committed, pushed, and deployed live on staging (`cebc3a1`/`e66c6ba`, Coolify deployment `ac82qg9h7wtawl2rgqyqa2md` confirmed `running:healthy`) — **Craig's own visual check at `localhost:3001` wasn't confirmed back before the push**; worth a look at staging next session (see Lane E cont. below for per-screen detail and the two caught/fixed bugs).
- [x] **Lane F (added 2026-07-20T19:22Z, completed 2026-07-20T~20:50Z): every remaining hub route brought to the same design-system standard.** All 12 units done — 4 via OpenCode CLI attempt (stalled/killed, see LANES note), all 12 actually delivered via Claude Code + Haiku subagents after the pivot, each independently verified by Claude Code (Sonnet) via `git diff` + `tsc`/`build`, not self-report. 3 real issues caught and fixed on review (see UNITS below for detail). Not yet pushed — `[GATE]`.

### Lane E (cont.) — Remaining hub screen mockups (added 2026-07-20 ~17:50, Craig-directed)
Six mockups dropped into `D:\apps\design-systems\brand-staging-2662e9`: `hub-dashboard.html`, `hub-exercise-library.html`, `hub-process-quality.html`, `hub-reports-updates.html`, `hub-sop.html`, `hub-studio-equipment.html`. All use the same token/class conventions as the already-shipped `hub-client-detail.html` (hub-card/hub-card-h/kpi/badge b-*, `--hub-card`/`--hub-border`/`--hub-canvas`/`--status-*` custom properties) — stylistically drop-in compatible. Scoping done by an Explore agent 2026-07-20; per-screen exclusions below come from that report. **Restyle only — no new routes, fields, filters, or workflows beyond what's listed as in-scope.**

- [x] `hub-dashboard.html` → `app/hub/(protected)/page.tsx`. **Done** — KPI band kept (already matched), added real-data `HubAlert` banners (danger/warning on compliance status) and a new "Recent check-ins" `hub-table`-style card from the real `activeSessions` query. Out-of-scope items correctly omitted (no Plan Agent weekly list, no dead buttons, no fabricated trend arrows). **Caught + fixed:** the unit's own edit left an unclosed `/* Recent check-ins */` comment (missing `*/}`) that broke `tsc` — found via full-project type check, not the unit's own self-report, and fixed directly.
- [x] `hub-exercise-library.html` → `exercise-browser.tsx`. **Done** — toolbar/cards/table reskinned to hub-card/badge tokens; all existing filters (archetype/movement/muscle group/equipment/source/difficulty) and bulk multi-select edit preserved. No mockup-only fields (tags, drawer content, "Add to plan") added.
- [x] `hub-process-quality.html` → `ProcessQualityManager.tsx`. **Done** — Register/SOPs/Improvement Log restyled to hub-card/table tokens, pill-tab nav with live counts. Fixed onboarding timeline and per-client checklist correctly omitted. All existing CRUD preserved.
- [x] `hub-reports-updates.html` → `UpdatesReport.tsx` + `page.tsx`. **Done** — KPI band/table/dialog reskinned. Bulk-select/"Send selected"/"Open rate" correctly omitted (don't exist today).
- [x] `hub-sop.html` → `ProcessQualityManager.tsx` (same file as process-quality — sequenced after it to avoid a concurrent-edit collision). **Done** — added a read-only SOP detail modal (meta grid + numbered Purpose/Scope/Procedure/What-good-looks-like/Prompt-template sections), every field mapped 1:1 to the real `Sop` type (ref/title/area/trigger/owner/steps/prompt_template/good_looks_like). No new fields fabricated, no new API routes.
- [x] `hub-studio-equipment.html` → `EquipmentManager.tsx` + `page.tsx`. **Done on retry** — first attempt planned the whole restyle in prose but silently never called Write/Edit (exited 0, files untouched — caught via `git diff`/mtime check, not the unit's self-report); re-dispatched with an explicit "you must call Write" instruction and it landed correctly. Pure reskin, add/toggle/delete functionality untouched.
- [x] Full-project `tsc --noEmit` run after all six units landed: clean except one **pre-existing, unrelated** error in `components/hub/ClientUpdatesPanel.tsx:60` (a `subtitle` prop typed `string` receiving a JSX element) — confirmed pre-existing via `git stash` (still present against a clean HEAD with no restyle changes applied). Not fixed — out of scope for this pass, flagged for Craig to decide.
- [x] `pnpm build`: compile step reports **"Compiled successfully"**; the build then fails during Next's `output: standalone` file-tracing step with `EPERM`/`symlink` errors — a pre-existing Windows/OneDrive filesystem permission limitation on this dev machine, unrelated to any code change (would fail identically on unmodified code).
- [ ] **Visual verification not done** — Craig chose to check it himself live rather than share hub login credentials for a browser-automation pass. Dev server was left running at `localhost:3001` during this session (may need restarting in a later session).
- [x] `git push` / deploy — **done, Craig's go-ahead 2026-07-20.** Commits `cebc3a1` (restyle) + `e66c6ba` (docs) pushed to `origin/main`. Coolify deployment `ac82qg9h7wtawl2rgqyqa2md` confirmed `finished`, app `running:healthy` on `https://staging.eternal-fitness.co.uk` — checked via Coolify MCP, not a self-report.

### Lane F — Full hub design-consistency sweep, remaining routes (added 2026-07-20T19:22Z)
Token system reference: `D:\apps\design-systems\brand-staging-2662e9\DESIGN.md` (Hub posture: p-5 cards, text-sm body, single shadow-sm, no hover lift — tighter density than the marketing site). Every unit: restyle only, preserve all existing functionality/data/filters/CRUD exactly; VERIFY always includes `npx tsc --noEmit` clean + `npm run build` compiles (the pre-existing `ClientUpdatesPanel.tsx:60` error and the Windows/OneDrive `EPERM` symlink step are known-unrelated, don't treat as new failures) + a functionality diff check (nothing added/removed beyond visual tokens).

- [x] `components/hub/PackagePaymentsCard.tsx` — swap `rounded-full` pill buttons for `rounded-lg`, matching the restyle already done on every other hub button (flagged, not done, since 2026-07-20T16:26Z). **Done** — 2-line change (Cancel + Save button classes), verified via `git diff` (isolated, no other change), `tsc`/`build` clean.
- [x] `app/hub/(protected)/clients/new/page.tsx` — restyle the create-client form to hub-card/token conventions matching the edit page (`hub-client-edit.html` pattern already shipped). **Done** — larger diff than a pure token swap (Contraindications/Pain Points moved from plain `Input` to `TagMultiSelect`, Training Location/Sessions-per-Week moved from `Select` to `SegmentedControl`); checked against `clients/[id]/edit/page.tsx` and confirmed both components/categories/data shapes are identical to the already-shipped edit page (not new/untested), `handleSave`/submission logic untouched, `tsc`/`build` clean.
- [x] Training delivery pages: `clients/[id]/blocks/[blockId]/page.tsx` + `.../review/page.tsx` + `.../print/page.tsx` + `.../sessions/[sessionNum]/page.tsx` — restyle to hub-card/badge tokens; print page keeps its print-specific layout (don't force hub chrome onto a print stylesheet). **Done** — Card→HubCard swaps, `rounded-full`→`rounded-lg` throughout, one shadcn `Alert`→`HubAlert` swap (verified: real component, matching `severity`/`title` props). Print page got only a 1-line token change as instructed, no structural change. `git diff` reviewed, no data/logic touched.
- [x] `clients/[id]/parq/page.tsx` + `parq/[parqId]/edit/page.tsx` — restyle to hub-card/table tokens matching the client-detail Compliance tab. **Done** — hub-card/table token restyle; edit page also gained a small client-name/back-link header (added one read-only `clients` lookup by `client_number`, `notFound()` if missing) — reviewed, low-risk addition matching the pattern used on other per-client pages, `ParqEditClient` (the actual read/edit logic) untouched.
- [x] `app/hub/(protected)/agreements/page.tsx` + `agreements/[id]/page.tsx` — restyle list + detail to hub-card/badge/StatusBadge tokens matching `DocumentRegister`. **Done** — list converted from flex cards to a table matching `DocumentRegister` conventions, all buttons `rounded-full`→`rounded-lg`; verified via `git diff` (no leftover `rounded-full`, no data/logic change) + `tsc`/`build` clean.
- [x] `app/hub/(protected)/documents/page.tsx` (top-level document register, distinct from the per-client documents tab) — restyle to match the already-shipped `DocumentRegister` pattern. **Done** — swapped local `Badge`+`STATUS_VARIANT` for the shared `StatusBadge` component, header cell padding aligned to `py-1.5`. Verified `StatusBadge`'s `documentStatusMap` covers all 5 statuses this page uses (draft/sent/received/signed/superseded) — no silent-disappear risk; `tsc`/`build` clean.
- [x] `settings/plan-agent/page.tsx` + `settings/training-rules/page.tsx` — restyle both settings screens to hub-card/form tokens (one unit, same area, sequence internally to avoid touching a shared settings layout concurrently). **Done** — also touched `PlanAgentSettingsManager.tsx`/`TrainingRuleTypesManager.tsx` (the actual rendered content), shadcn `Table`→native table, hub field/button tokens throughout. `git diff` reviewed — all save/toggle/add logic (`addRuleType`, `toggleActive`, `saving`) untouched, pure markup/class changes.
- [x] `site-content/page.tsx` + `site-content/[slug]/page.tsx` — restyle CMS list + editor to hub-card/table tokens. **Done** — Card→HubCard across list/table/editor. Mid-edit the file had a transient unclosed-JSX error (caught by a concurrent unit's build check); confirmed resolved once this agent finished — `tsc` clean now. `page_content_blocks` save/load logic untouched.
- [x] `site-review/page.tsx` — restyle to hub-card tokens. **Done** — 6-tile KPI band via `KpiTile` (statusToken choices checked, semantically correct: Total→primary, Completed→success, Critical Issues→danger, Est. Hours→neutral), Card→HubCard, tabs restyled. `git diff` reviewed, no data/logic touched.
- [x] `templates/page.tsx` + `templates/[id]/TemplateEditorClient.tsx` — restyle document-template list + editor to hub-card/table tokens. **Done, two fixes applied on review** — agent had mapped template active/inactive and the PAR-Q "Form" tag onto `StatusBadge status="signed"`, which would've rendered a misleading green "Signed" badge for an active template/PAR-Q form (conflating template-active with document-signed status); fixed to `status="active"` (resolves correctly via `blockStatusMap`) and a plain neutral "Form" badge respectively. Editor (`TemplateEditorClient.tsx`) is a clean Card→HubCard swap, `addSection`/`move`/`removeSection`/`save` untouched.
- [x] `tracker/page.tsx` — restyle to hub-card/kpi tokens matching the dashboard's KPI band. **Done, one fix applied on review** — table header `font-medium`→`font-semibold` accepted; agent also dropped the `sm:` responsive prefix on the KPI grid (`sm:grid-cols-3`→`grid-cols-3`), incorrectly claiming it matched the dashboard (dashboard actually uses `lg:grid-cols-3`, still responsive) — reverted to `sm:grid-cols-3` to keep the 3 tiles stacking on narrow viewports. Data/queries untouched.
- [x] Hub auth screens: `hub/login/page.tsx`, `hub/forgot-password/page.tsx`, `hub/reset-password/page.tsx` — restyle to brand tokens (these sit outside `.hub-shell` density rules — closer to the marketing-site register per DESIGN.md). **Done, two fixes applied on review** — agent correctly moved to the marketing-site register (white canvas, DM Serif Display, 48px pill buttons per DESIGN.md) but used two classes that don't exist in this project's Tailwind config: `font-400` (not a real Tailwind weight utility — no-op) and `text-muted-text` (no such color key registered, only `muted`/`muted-foreground` — no-op); both would have silently failed to apply. Fixed to `font-normal` and `text-[var(--color-muted-text)]` (matching the bracket-syntax convention used for this CSS var elsewhere in the codebase) across all 3 files, 9 occurrences. Auth logic untouched, `tsc` clean after fix.
- [x] Full-project `npx tsc --noEmit` + `npm run build` after all Lane F units land. **Done** — clean except the two known pre-existing/unrelated issues (`ClientUpdatesPanel.tsx:60` prop-type error, Windows/OneDrive symlink build step). All 12 units verified individually via `git diff` review (not just self-report) — 3 real issues caught and fixed on review: a mislabelled `sm:` breakpoint removal on tracker's KPI grid, a `StatusBadge` semantic misuse on the templates page (signed↔active conflation), and two no-op Tailwind classes (`font-400`, `text-muted-text`) on all 3 hub auth screens.
- [x] `git push` / deploy of the accumulated Lane F commits — **done 2026-07-21, Craig's go-ahead.** All 21 files pushed as two commits (`be45c83` restyle batch, `0e3d65a` docs), verified re-clean (`tsc`/full build) before push since time had passed since the original Lane F session. PAR-Q edit page's approach (breadcrumb/header wrapper around the shared `ParqEditClient` form, not touching the form's own internals) held up under a second look — it was the safe pattern, not a risk.

### Lane G — Icon/status-colour audit vs OpenDesign mockups (2026-07-21, Craig-directed, not in original scope)
Craig flagged a real design/implementation drift on the Site Content page (icons and colours swapped between "Needs Writing"/"Needs Updating") and asked whether it was systemic. It was. Dispatched 8 parallel Explore agents, one per hub page with a source mockup in `D:\apps\design-systems\brand-staging-2662e9`, each diffing icon shapes and `--status-*` token usage against the mockup HTML. 6 of 8 pages had real, previously-unnoticed defects — not cosmetic in every case:
- **Dashboard**: `StatusBadge status="success"` passed a token name instead of a real status value, so `lookupStatus()` returned null and the badge silently rendered nothing on every "Recent Check-ins" row. Also found a literal `/* Needs Attention */` JS comment sitting unwrapped in the JSX tree, rendering as visible text on the page (separate bug, caught by Craig after the icon fixes shipped).
- **Reports & Updates** (`lib/updates/status.ts`, feeds both `/hub/reports/updates` and the client-detail Updates tab): update-email statuses were mapped to raw shadcn `Badge` variants instead of the shared tokens — "Sent" resolved to white-on-white (invisible), "Draft" rendered teal instead of amber, "Scheduled" a plain outline instead of rose.
- **Client detail**: 3 card-header icons had no/wrong `color` prop (Active Block, Training Blocks, 6-Week Updates), GP Clearance badges used raw shadcn variants (same invisible/wrong-colour pattern), Plan Agent bot was navy throughout instead of teal, Profile tab used the group icon instead of a single-person one.
- **Client edit**: 5 of 8 card-header icons didn't match (group vs single-person, map-pin vs calendar, dumbbell vs ruler, alert-circle vs shield-check, generic clipboard vs the Plan Agent robot icon the mockup deliberately reused).
- **Process & Quality**: SOPs KPI tile was success/teal with a book icon when the mockup specifies neutral/file — the correct file icon was already used two lines away in the same component. Also fixed an internal inconsistency (local "draft" badge rendered rose when `lib/hubStatus.ts` defines draft as neutral everywhere else).
- **Exercise library**: one card-header icon (dumbbell instead of a list glyph) — genuinely cosmetic-only.
- **Clean, no changes needed**: `/hub/clients` (only an unrelated missing `sortable: true` on the Conditions column, fixed anyway), `/hub/studio-equipment`.

Root cause behind most of the real (non-cosmetic) bugs: several pages had their own disconnected status-colour system (raw shadcn `Badge` `variant` props) instead of the shared `lib/hubStatus.ts`/`StatusBadge` tokens — introduced during the very restyle passes documented above. Fixed by extracting a shared `TokenPill` component (`components/hub/StatusBadge.tsx`) for status domains whose string values collide with the global `lookupStatus()` chain (e.g. "sent"/"draft" already mean something different for documents), and moving every raw-Badge call site onto it or `StatusBadge`.

Also fixed two shared-component bugs affecting every hub page that uses them: `HubCardHeader`'s icon badge was 36px against the mockup's 30px everywhere; `HubAlert`'s danger severity used the same triangle icon as warning (so "Do Not Train" and "Action needed" were only distinguishable by colour, not shape) — now uses a circle-alert icon per the mockup, which was already imported and unused. Also fixed `HubCardHeader`'s `subtitle` prop, typed `string` but receiving JSX from at least one caller — this was the actual cause of the `ClientUpdatesPanel.tsx:60` TypeScript error flagged as "pre-existing, unrelated" three times across this Work Order's history; full-project `tsc --noEmit` is now completely clean for the first time.

All fixes verified (`tsc --noEmit` + full `next build`, both clean except the known Windows/OneDrive symlink step) and deployed live across 3 commits (`ad827cf` icon/colour audit, `1ff7335` the literal-comment bug). No new icons invented without cause — `IconUser`, `IconShieldCheck`, `IconRuler` added to the icon set only where the mockup specified a shape genuinely absent from the existing 90+ icons.

### Lane H — Full mockup-to-route visual/IA alignment pass (2026-07-21, later session, Craig-directed)

Craig asked for a systematic pass bringing every hub route in line with its `hub-*.html` mockup — a
broader, more deliberate version of Lane G's audit, this time covering all 10 mapped routes at once
rather than reacting to one spotted defect. Two real decisions were surfaced during pre-brief and
resolved by Craig before any implementation:

1. **`hub-process-quality.html` mismatch** — the mockup invents an onboarding-checklist/safeguarding
   page, entirely different from the real Process Register/SOPs/Improvement Log CRUD tool (10 real
   published SOPs, DB-backed). **Decided: discard the mockup's content, reuse only its visual shell.**
2. **`hub-dashboard.html` data delta** — mockup shows a simplified sessions/check-ins/alerts/weekly
   layout with different terminology than the real Supabase-backed KPIs (Total Clients, Draft/Active/
   Approved/Total Blocks, Do-Not-Train alerts, Recent Check-ins, etc). **Decided: re-skin only, keep
   every real metric/field/terminology exactly as-is.**

Dispatched 12 parallel agents (one per route + one for sidebar nav IA), each told to verify before
assuming work was needed — most of these routes were already restyled in Lanes E/F/G. Findings:

**Already matching, no changes needed** (verified against mockup, not assumed): Dashboard, Clients
list, Client detail, Client edit, Studio Equipment, Email Updates (Reports), Exercise Library, Process
& Quality (shell only — confirmed all 3 real CRUD tabs and their data untouched, no mockup checklist
content added).

**Real fixes applied:**
- **All Documents** (`app/hub/(protected)/documents/page.tsx`) — the mapped mockup `hub-sop.html`
  turned out to actually be an SOP *detail*-page layout, not a documents list (brief's mockup mapping
  was wrong) — flagged as a background task for Craig rather than guessed at. Instead brought in line
  with the hub's own established list-page pattern already proven on `tracker`/`clients` (shared
  `HubPageHeader`, `HubCard`, `Table` primitives, KPI tiles with semantic status tokens) — same tokens,
  consistent with every other list page, just not verified against a real "All Documents" mockup.
- **Site Content list** — one real delta: the Type column (Static/Condition/Blog/Legal) hand-rolled
  its own pill instead of the shared `TokenPill` component every other status pill in the hub uses.
  Fixed in `site-content-table.tsx`.
- **Site Content editor** — real content bugs, not just styling: a literal `&amp;amp;` rendering as
  visible text in the "SEO & Keywords" card header (double-escaped, not `dangerouslySetInnerHTML`);
  missing icon on that card header; several field labels in Title Case against the sentence-case
  convention used across ~15 other hub pages; missing descriptive subtitle under "Content Blocks".
  Fixed in `site-content-editor.tsx`. Deliberately did NOT add the mockup's `.card-actions` footer
  band — no other hub card uses that pattern, so it would've been a one-off, not an alignment fix.

**Sidebar nav grouping** — checked, not changed. None of the 11 mockups actually show "Process &
Quality" in any sidebar at all, and the mockups' own grouping is internally inconsistent (Resources
renamed to "Library" in one file, missing entirely in others, Settings item counts vary) — reads as
unfinished draft copies, not a deliberate alternative IA. No evidence-based target to move to, so left
under Resources as-is; flagged for whoever owns the mockup set.

**Out-of-scope items found and spawned as separate background-task suggestions** (not actioned here):
client-edit page is missing the mockup's right-hand summary rail + outstanding-clearance banner (needs
new computed logic, not a style change); `ProcessQualityManager.tsx`'s `registerStatusBadge()` hand-
rolls badge markup instead of reusing `TokenPill` (cosmetically identical today, just duplicated code);
the `hub-sop.html`/All-Documents mockup mismatch above.

**Verified:** full-project `npx tsc --noEmit` clean after all agents' combined edits. Not yet committed
or pushed, not live-browser-verified (no hub credentials this session).

### Lane I — Scanned/paper document storage (added 2026-07-22, Craig-directed)
Craig has a paper Personal Training Agreement for Sarah (`eternal-fitness/clients/sarah/Personal_Training_Agreement_2026 (1).pdf` — appears to be a scan, no extractable text via `pdftoppm`). Decision: store the original as a copy against her record now, rather than transcribing into the `terms` document-engine body — no file-storage/attachment capability exists anywhere in the hub today, this is genuinely new, not a reuse of an existing upload path.
- [x] Add `source_type`/`source_file_name`/`source_file_mime`/`source_file_size` columns to `client_documents` (migration, additive, no backfill needed — every existing row is `'generated'`). **Done 2026-07-22** — `supabase/migrations/20260722_scanned_document_storage.sql`, written in worktree `D:\apps\worktrees\eternal-fitness-website-lane-i-scan-storage` (branch `task/lane-i-scanned-document-storage`), not yet run against prod.
- [x] Storage path — decided against a Coolify volume (unnecessary infra for a single-digit-documents volume); bytes stored directly in Postgres via a new `client_document_files` table (`document_id` PK/FK, `data BYTEA`), served back only through an authenticated hub API route. **Done 2026-07-22**, same commit. **RLS bug found and fixed live**: the original migration's `CREATE POLICY ... TO authenticated` failed against prod — `authenticated` is a Supabase role that never carried over to this plain-Postgres instance; confirmed the existing `client_documents` table has RLS enabled with **zero working policies** for the same reason (access control is enforced at the app layer, not Postgres RLS). Migration fixed to match that real, working pattern (commit `6bd17d2`) rather than add a second broken statement.
- [x] "Upload existing document" action on the client Documents tab (`DocumentHeaderActions.tsx`/`DocumentUploadForm.tsx`) — kind selector + signed-date + file picker (PDF/PNG/JPEG, 10MB cap), sets `source_type='scan'`, `status='signed'`. Scanned rows in `DocumentRegister` show a "Scanned original" `TokenPill` and a "Download original" link instead of send/resend/view (nothing left to sign). `POST /api/documents/upload` + `GET /api/documents/[id]/file`, both staff-auth-gated. **Done 2026-07-22** — `npx tsc --noEmit` clean, `npm run build` compiled successfully, independently re-verified via `git diff` (not just self-report; caught and reverted an incidental `package-lock.json` churn from local `npm install` — this repo actually deploys via `pnpm --frozen-lockfile`, so that file is dead weight, not the real lockfile).
- [x] Pushed/merged (`dd93c2a` then RLS-fix `6bd17d2`, fast-forward from an isolated worktree per DO-SOP-010) and migration run against production. **Done 2026-07-22** — verified live: 4 new columns on `client_documents`, `client_document_files` table exists, all 30 pre-existing `client_documents` rows unaffected. Both Coolify deployments (`cpgy4t0jcyi8i86zxm1k9fww`, `f31yh7dychgc92u3b2oxzr04`) confirmed `finished`/healthy on first healthcheck attempt via Coolify MCP, not self-report.
- [x] Sarah's actual PDF uploaded and her `client_documents` row created. **Done 2026-07-22** — id `a74a1ef7-0c19-478c-b5e2-538a9304e102`, `kind='terms'`, `status='signed'`, `source_type='scan'`, 183,462 bytes, verified byte-for-byte (stored blob length matches source file length exactly). Uploaded via a one-off script against prod (no live hub session this session to use the UI upload form) — the UI upload path itself is untested against a real browser session, worth a quick click-through next time the hub is open logged in. `client_signed_date` left unset (scan has no extractable text/date, `pdftoppm` not installed to OCR it) — Craig can set it from the paper original if wanted.

### Lane J — Paper→digital conversion tool — **SCOPED 2026-07-22, recommendation below, not built**
Craig's ask: once a paper document is stored (Lane I), be able to turn it into something the plan-generation agent can actually use. Two genuinely different outputs were on the table:

- **Option A — OCR/transcribe into the document engine's HTML `body`.** Preserves document fidelity (renders/prints like a normal `terms` document) but the plan-generation agent doesn't read document HTML today — `ClientProfile` (`types/index.ts`) is what it actually consumes, so this wouldn't make the agent any smarter on its own. Lower value against Craig's stated goal.
- **Option B — extract key fields straight into `clients`/`ClientProfile`.** Checked what the agent actually reads: `ClientProfile.health` already has `conditions`/`contraindications`/`medications_relevant`/`injury_history`/`pain_points`, and `logistics.package` already exists — exactly the shape a Personal Training Agreement's health disclosures and package terms would fill. **No new `clients` columns needed for the common case** — this is populating existing fields, not building new ones. Directly useful to the agent, which is what Craig actually asked for ("so we can get the agent to use it when it generates plans").

**Recommendation: Option B**, for the reason above — it's what the agent can act on, and it reuses fields that already exist rather than adding scope.

**Real technical blocker found while scoping, not previously flagged**: both options need to *read* the scan first, and Sarah's PDF has no extractable text (`pdftoppm` isn't installed on this machine to even render it to an image for OCR) — traditional OCR isn't available in this environment today. **Better fit than installing OCR tooling: a vision-capable LLM call** (Claude or another vision model via the existing `OPENROUTER_API_KEY`/`ai-client.ts` integration already used for plan generation) reading the scan image directly and returning structured fields — skips OCR entirely, and reuses infrastructure that already exists rather than adding a new one. Caveat: `ANTHROPIC_API_KEY` is currently empty in prod (existing Known Issue, plan generation already falls back to template because of it) — this would need that resolved, or to run entirely on OpenRouter instead.

**Human review is a hard requirement, not optional** — this is health/medical/financial data feeding a training-plan agent; nothing extracted from a scan should write to a client's live profile without staff confirming it first. Shape: extraction produces a staged diff (proposed field values) on a review screen, staff accepts/edits/rejects per field, only accepted values get written — never a silent auto-write.

- [ ] Build Option B: vision-LLM extraction → staged review diff → staff-approved write to `clients`/`ClientProfile` fields. **Not started** — needs Craig's go-ahead to build (touches the same client-profile fields the plan agent depends on) and, separately, a decision on the `ANTHROPIC_API_KEY` gap.

### Lane K — Portal auth rework: magic-link → password + reset — **BUILT + VERIFIED 2026-07-22, not yet shipped**
Craig tried the existing portal login and got no email. Real cause found while scoping this, not previously known: **no `portal_*` tables exist on production at all** — the 2026-07-20 magic-link migration was written but genuinely never run, so the portal login has never worked against real data (not the SMTP dry-run issue, not anti-enumeration — a missing-table failure). No legacy portal accounts to migrate; clean slate. Separately, Craig wants a different model entirely: no self-serve request surface at all; Esther adding/inviting a client is what creates their login and sends it; clients can only self-serve a password reset afterward.
- [x] Design the replacement: `portal_accounts` gains `password_hash` (Node's built-in `crypto.scryptSync`, no new dependency — deliberately not sharing `lib/auth.ts`'s `better-auth` hashing, since the portal auth surface is intentionally isolated from staff auth per that file's own header); account creation happens only via a new staff-triggered "Invite to portal" action — closes the real gap in `ensurePortalAccount()` (removed) that used to auto-create *and* auto-enable an account on any matching-email request. **Done.**
- [x] Built in isolated worktree `D:\apps\worktrees\eternal-fitness-website-lane-k-portal-auth` (branch `task/lane-k-portal-auth-password`, commit `65b5ee6`): `lib/portal-auth.ts` rewritten (`invitePortalAccount`/`verifyPortalLogin`/`requestPasswordReset`/`resetPortalPassword`); new API routes `/api/portal/auth/{login,logout}` + staff-gated `/api/clients/[id]/portal-invite`; `request-link`/`verify` repurposed for reset request/submit; new `/portal/forgot-password`/`/portal/reset-password` pages; login page rewritten as email+password with a forgot-password link; `middleware.ts` portal guard updated to allow the two public reset-flow paths. "Invite to portal" button added to the client detail Profile tab, disabled when the client has no email on file. **Done 2026-07-22.**
- [x] Migration rewritten in place (unrun, so no existing-shape constraint): `portal_magic_links` → `portal_reset_tokens` (reset-only purpose now), `password_hash` column added, **no RLS policies referencing the nonexistent `authenticated` role** — same fix as Lane I, matching the app's real access-control pattern (enforced at the API-route layer, not Postgres RLS). **Done.**
- [x] Independently re-verified (not self-report): `git diff` review of all 13 changed/new files, confirmed no leftover references to the removed magic-link functions (`requestPortalMagicLink`/`verifyPortalMagicLink` — grepped, zero hits), confirmed the protected layout's existing logout form action (`/api/portal/auth/logout`) is now backed by a real route instead of hitting the old verify route's repurposed body. `npx tsc --noEmit` clean, `npm run build` compiled successfully (one real bug hit and fixed during the build itself: `useSearchParams()` needed a `Suspense` boundary on both `/portal/login` and `/portal/reset-password` or the static export failed — fixed, confirmed clean on rebuild).
- [GATE] Pushing/merging this branch, running the migration, and deploying — **this is the security-surface change the Work Order's own ASK FIRST list calls out for explicit confirmation, not a routine ship.** Built and verified; holding here for Craig's go-ahead specifically on this lane before it goes live, distinct from the general "get this done" instruction that shipped Lane I.
- [GATE] Sending any real "Invite to portal" email to a real client.
- [GATE] Sending any real "Invite to portal" email to a real client.

## LEDGER
Progress written per unit to `eternal-fitness-website/.context/handoff.md` (all lanes) and this Work Order's DONE checklist. Add a row to `infrastructure/.context/active-workorders.md` when this moves from DRAFT to ACTIVE.

## CONTEXT
- **Source docs:** `EF_Trainerize_Accessibility_Scope_Jul2026.md` (2026-07-15, OneDrive workspace repo) — the existing 4-phase accessibility/commercialisation charter; its Phase 1 (session logging/plan delivery) and Phase 2 (accessibility-first UX) are folded into **Phase 3** of this Work Order, not dropped. `charter-document-engine.md` (OneDrive workspace repo, `.context/`) — the document engine's own build charter, Phase 4 (PAR-Q migration) is Lane C here.
- **Process & Quality System reality check:** it's a hardcoded `.tsx` component with no database backing and no admin UI — found in `decoded-ops-hub` and copy-pasted into `decoded-data-app` for TackleBag. Porting it is a UI-shell copy + fresh content-writing job, not a plug-in install. Flagged as an open decision in Lane B on whether to leave it hardcoded or add a DB-backed editor.
- **Client data reality check (as of 2026-07-20):** `clients` table exists with real data; PAR-Q import is substantial and working (`scripts/import-parq.mjs`, 13+ clients ported); Trainerize integration to date is exercise-library only, no client-data import exists; document engine (`document_templates`/`client_documents`) is live and already covers T&Cs, Risk Assessment, and Annual Review.
- **Prod DB access pattern:** Coolify SSH tunnel, `127.0.0.1:5433` → `10.10.10.2:5432`, role `ef_app` — tunnel opened by Craig each session, password handled inline per script invocation, never persisted to disk. Every write needs Craig's explicit go-ahead that session, not inferred from Work Order approval alone.
- **Two VI clients exist today** but neither is expected to use the portal immediately — this is why Phase 2 stays document-only and Phase 3's deeper accessibility work (voice logging, SR-tested plan read order) is deliberately deferred rather than gold-plated up front.
