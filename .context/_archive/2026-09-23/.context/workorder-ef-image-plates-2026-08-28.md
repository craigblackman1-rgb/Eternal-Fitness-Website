# Work Order: Eternal Fitness — Image plates (CR-EF-086)

**Slug:** `wo-ef-image-plates-2026-08-28`
**Status:** ACTIVE
**Owner:** claude (this session)
**Apps:** eternal-fitness-website
**Scope:** CR-EF-086 — site-wide VI (visual impairment) accessibility pass. Craig's decision 2026-08-28: build both (a) real screen-reader-quality alt text site-wide and (b) the full `image-treatment-plates.html` visual grading system site-wide, replacing the current `/visual-impairment`-only pilot.

Source mockups (Open Design project `90556eb1-3632-4e02-a992-bc510026774c`, delivered 2026-08-25):
- `photo-approval-plates.html` — alt-text descriptions, 11 new-shoot photos
- `image-treatment-plates.html` — 5 treatments (EF-IMG-01..05) + 6-rule a11y layer (EF-A11Y), with an application map and effort estimates
- `homepage-image-plates.html` — homepage-specific plates
- Existing partial pilot: `app/visual-impairment/vi.css` (4 of 5 treatments, ported 2026-08-11, scoped to `.vi-page` only)

---

## DONE (definition)

- [x] Alt-text audit complete (`.context/alt-text-audit-2026-08-28.md`) — but corrected the premise: 33/38 sites already descriptive, only 5 have a suboptimal title-fallback pattern. No new alt-text copy was authored/wired (audit-only per brief; authoring is a follow-up, not blocked on anything).
- [x] EF-IMG-01 (studio grade) applied to every photograph site-wide via shared tokens — confirmed on homepage, About, and every `ds-*`-component page.
- [~] EF-IMG-02 (session plate) — applied to homepage/CTA process-step containers (`.ds-step-img`/`.ds-flow-step-img`, shared selector) and one Hub exercise detail image. **Not applied**: the "who it's for" grid, testimonials (has no photos — N/A, documented), the full Hub exercise library grid (only the single detail-view image got it).
- [x] EF-IMG-03 (the split) applied to the 6 launch-page heroes + CTA bands (About/Contact/Personal Training/Specialist Training/FAQs/Pricing) as an explicit opt-in — homepage, Testimonials, and Visual-Impairment deliberately excluded (see Lane 3 ledger entries for why).
- [~] EF-IMG-04 (focus ring) — applied to the 3 homepage process-step images with real focus points. **Not applied**: Hub exercise thumbnails (lane's own judgement call: too small for the treatment at 56px), other "technique imagery" site-wide.
- [~] EF-IMG-05 (the mount) — `SpecialistGrid.tsx` covered, but that component is currently unused anywhere in the app. **Not applied**: testimonial cards, pricing cards, email signature, case-study headers.
- [~] EF-A11Y six-rule layer — contrast repairs (`--color-rose`→`--rose-text`) applied site-wide. Described-images `.ef-desc` pattern ported but not yet applied to any specific image (no image judged to need it yet).
- [x] Design Parity — every lane's diff hand-reviewed against the mockup's own CSS/values (not a formal side-by-side screenshot diff, since the mockups are Open Design HTML files, not rendered comparables at each breakpoint).
- [~] Live-verified in browser on staging (desktop only — no mobile-viewport pass was done this session).

---

## LANES

### Lane 6 — [GATE→Open Design] Re-plate the REAL site photographs (raised by Craig 2026-08-28, post-test)

**The structural problem, found by Craig on staging:** the existing plates cover photographs that are not on the site, and the photographs that ARE on the site have no plates. `photo-approval-plates.html` authored rule-4-quality alt text for 11 new-shoot photos — none of which were ever uploaded (deferred, `dmtd2xq1coh`). Meanwhile the **~35 significant photographs actually live on the site** have never been through a plate at all.

**Measured state (2026-08-28, live routes only — excludes the redirected `/cancer-rehabilitation`, `/falls-prevention`, `/blog`):**

| Route | Significant photos | Rule 3 (described images) | Rule 4 (alt standard) |
|---|---|---|---|
| `/visual-impairment` | 6 | ✅ 3 `.vi-desc` disclosures | ✅ passes |
| `/` (home) | 7 | ❌ none | ⚠️ partial |
| `/about` | 7 | ❌ none | ⚠️ partial |
| `/contact` | 3 | ❌ none | ❌ "Esther Fair smiling" |
| `/personal-training` | 3 | ❌ none | ⚠️ partial |
| `/specialist-training` | 3 | ❌ none | ❌ "Specialist personal training in Worthing" (SEO string) |
| `/faqs` | 2 | ❌ none | ⚠️ partial |
| `/pricing` | 2 | ❌ none | ⚠️ partial |
| `/testimonials` | 2 | ❌ none | ⚠️ partial |

**Rule 3 is unimplemented on 8 of 9 live routes** — `.ef-desc` CSS shipped in Lane 5, applied to zero images. Craig is right that heroes and full-bleed images are the worst offenders: they're the largest, most significant photographs on every page and carry the least description.

**Rule 4 grading correction — own this:** Lane 5's audit reported "33/38 already descriptive" and I accepted it. That graded against *"is alt present and non-generic"*, **not** against the mockup's actual Rule 4 standard — *"what is happening, who is in frame, and what the adaptation is."* Re-graded against the real rule, most alt text names the subject but never the **adaptation**, which is the distinctive third element and the entire reason this is Eternal Fitness's differentiator rather than generic a11y compliance. `"Specialist personal training in Worthing"` scores zero of three. That miss is why this gap didn't surface in the audit.

**Units:**
1. **[GATE] Craig confirms scope** — all ~35 photos, or heroes/full-bleed first (the ~18 largest) as a phase 1?
2. **[Open Design] Author a new plates file covering the real site inventory.** Must be Open Design, not an OpenCode lane and not me: writing a truthful description of what is in a photograph requires *looking at the photograph*. Inferring it from a filename is exactly how fabricated content gets shipped (standing repo gotcha — the "Joan"/"Section 7" incidents). Per photo: rule-4 alt text + the `<details>` described-image copy.
3. **[AUTO once plates approved] Wire it in** — `.ef-desc` disclosures under significant photographs; replace failing alt strings. Mechanical once the copy exists.
4. **Constraint for both:** brand vocabulary ban applies to alt text too (Rule 4) — never `transformation`, `before-and-after`, `weight loss`, `aesthetic`, `quick fixes`, `boot-camp`, `push yourself`, `no pain no gain`, `gym-floor intimidation`, `just push harder`; no body descriptions.
5. **VERIFY:** every significant photograph on a live route has rule-4 alt + a described-image disclosure; screen-reader pass; `prefers-contrast: more` still correct.

### Lane 1 — [AUTO] Foundation: shared tokens + studio grade + a11y contrast layer

Promote the `--vi-img-*`/`--vi-vignette`/contrast tokens currently defined inside `.vi-page` in `app/visual-impairment/vi.css` into a site-wide home (`app/design-system.css` or `app/globals.css`) under non-`vi-`-prefixed names (e.g. `--ef-img-grade`). Apply EF-IMG-01's studio-grade filter to every photograph site-wide (four selectors per the mockup's effort note). Wire the `prefers-contrast: more` media query site-wide, not scoped to `.vi-page`. Do **not** touch `/visual-impairment` page's own behaviour — it should keep working, now reading from the promoted tokens instead of its local ones.

**MUST:** land first — every other lane in this WO builds on these tokens.
**VERIFY:** every photo on the homepage still renders (no missing filter var), `/visual-impairment` page unchanged visually.

### Lane 2 — [AUTO, depends on Lane 1] EF-IMG-02 session plate + EF-IMG-05 the mount

Apply `.vi-plate`-equivalent treatment (ticks + opaque-ink caption) to: "who it's for" grid, process steps, testimonials, About, Hub exercise library. Apply `.vi-mount`-equivalent treatment (caption below, held-back mat) to: Esther's portrait, testimonial cards, pricing cards, email signature, case-study headers. Reference `image-treatment-plates.html` sections 02 and 05 for exact visual spec.

### Lane 3 — [AUTO, depends on Lane 1] EF-IMG-03 the split (page heroes + CTA band)

Bigger layout change (per mockup: "a day — a layout change, not a filter"). Replaces the scrim stack and `--pan` hack on the six page heroes (`PageHero.tsx`) and `CTABand.tsx`. Reference `image-treatment-plates.html` section 03. Watch the known crop traps already documented in that mockup: the `.ds-split-img` four-ratio override problem on About, and the hero's `--hero-pos`/`--hero-pos-wide` split.

### Lane 4 — [AUTO, depends on Lane 1] EF-IMG-04 focus ring

Homepage process steps, technique imagery, Hub exercise thumbnails. Needs a real focus point per image (not default-centre) — pull from `image-treatment-plates.html`'s focus-point data if specified per image, otherwise flag images needing a manual point as a deferred follow-up rather than guessing.

### Lane 5 — [AUTO, depends on Lane 1] EF-A11Y site-wide + alt-text wiring

1. Wire the 11 `photo-approval-plates.html` descriptions onto their matching site images.
2. Audit every remaining image in `public/images/` and every `<img>`/`next/image` call site for missing/generic alt text (Claude does this authoring pass directly — content, not app code — before handing wiring to the lane).
3. Apply the described-images `<details class="vi-desc">` pattern site-wide (currently `/visual-impairment`-only).
4. Promote the contrast-repair rules (`.vi-page .ef-btn-primary` etc.) site-wide where the same AA-fail pattern exists outside `/visual-impairment`.

---

## LEDGER

- 2026-08-28: WO created. CR-EF-086 register reconciliation done (see `workorder-ef-consolidated-2026-08-27.md` D11). Craig chose "alt-text + full visual rollout" scope. Lanes scoped from `image-treatment-plates.html`'s own application map (EF-IMG-01..05, EF-A11Y).
- 2026-08-28: **Lane 1 shipped to staging, live-verified.** Dispatched to OpenCode, diff hand-reviewed (2 commits — foundation tokens + a homepage follow-up caught on live verify: `.efhome`'s bespoke classes in `home.css` weren't covered by the shared `ds-*` selectors), `tsc` clean both times. Live-verified via Craig's real Chrome session on `development.eternal-fitness.co.uk` (Cloudflare Access blocks the sandboxed browser pane): `/visual-impairment` computed filter unchanged, homepage's 5 image classes now carry the studio-grade filter, no console errors. **Production promotion queued** (`qmtd2vl6vgb`) — not pushed to `main` without Craig's go-ahead. Lanes 2-5 dispatched in parallel off `origin/staging` (which now carries Lane 1's tokens).
- 2026-08-28: **Real blocker found and deferred (`dmtd2xq1coh`):** the 11 new-studio-shoot photos in Open Design's `photo-approval-plates.html` (exact filenames confirmed from the mockup's own `<p class="filename">` captions) were never uploaded to `public/images/` — repo has similarly-named but different existing files. Lane 5 rescoped to drop the alt-text-wiring half of its brief; needs Esther/Craig to upload the photos (or confirm they replace specific existing files) before that sub-item can build.
- 2026-08-28: **Lane 4 (focus ring) shipped to staging.** Diff hand-reviewed — faithful port of `.vi-tech` into a generic `.ef-tech`, applied to the 3 homepage process-step images with real focus points and instructive pin labels; lane correctly declined hub exercise thumbnails (documented reasoning: too small for the treatment, not a silent skip). `tsc` clean, live-verified in browser (computed styles match spec, no console errors), pushed to staging.
- 2026-08-28: **Lane 5 (a11y layer + alt-text audit) shipped to staging.** Diff hand-reviewed — `--color-rose` → `--rose-text` contrast-repair swap applied only to genuinely text-carrying/focus-ring uses site-wide, decorative-only rose uses correctly left alone. Described-images `.ef-desc` CSS ported but not yet applied anywhere (no image currently judged to need it). **Alt-text audit corrects CR-EF-086's founding assumption** — 33/38 image call sites already have descriptive alt text; only 5 use a non-ideal title-fallback pattern, 0 missing/filename-derived. Merge conflict with Lane 4 (both touched `design-system.css`) resolved manually — additive, non-overlapping blocks. `tsc` clean, live-verified (`.ef-btn-primary` computes `--rose-text` correctly, Lane 4's treatment unaffected), pushed to staging.
- 2026-08-28: **Lane 3 (the split) built but held back — real scope problem caught on review, not merged.** The lane made `layout="split"` the *default* for every `PageHero`/`CTABand` caller (10 and 9 call sites respectively) instead of opt-in — this would have silently changed `/visual-impairment`'s hero too, which no lane in this WO may touch without an explicit decision (my own brief-writing gap, not the lane's fault — I never said this to Lane 3). Follow-up dispatched: flip the default back to `overlay` (zero behaviour change site-wide by default), explicitly opt in only the 6 CLAUDE.md-named launch pages (About/Contact/Personal Training/Specialist Training/FAQs/Pricing), explicitly leave Testimonials/Visual-Impairment/the two dead redirected routes untouched. Not yet re-reviewed.
- 2026-08-28: **Lane 2 (session plate + mount) built but held back — real regression caught on review, not merged.** `SpecialistGrid.tsx`'s mount-treatment swap silently deleted the numbered index badge ("01"/"02"/"03") and the staggered card layout, neither of which was asked for — content loss, not a style change. Coverage also incomplete: testimonials and About-page imagery (both explicitly named in the brief) were never touched, no reasoning given. Follow-up dispatched: restore the index number + stagger (or document why not), extend to testimonials + About. Not yet re-reviewed.
- 2026-08-28: **Lane 2 follow-up shipped to staging, live-verified.** Index/stagger restored correctly (uses `--rose-text` per Lane 5's contrast pattern). Testimonials/About gaps resolved with legitimate documented reasoning (testimonials has no photos at all — initial-letter avatars only; About images already covered via `.ds-split-img`'s shared selector). Noted: `SpecialistGrid` is currently unused anywhere in the app (confirmed via grep, no import sites) — zero live-site risk either way, but worth having correct for whenever it's wired up. `tsc` clean, pushed to staging.
- 2026-08-28: **Lane 3's first fix (isolation) was insufficient — a second, deeper bug found and fixed on live verification, not caught by any automated check.** Loaded `/about` in a real (non-headless) browser: the split hero's photo rendered solid black even after the `isolation: isolate` fix. Diagnosed via devtools live injection (disabling the wash `::after` overlay immediately revealed the real photo) — root cause: `.ds-hero-split-photo`/`.ds-cta-split-photo` were the only two treatment containers in the whole WO that never set `background: var(--color-ink)` on themselves, unlike every working equivalent (`.ef-mount-win`, `.ef-plate`, `.ef-tech`, `.ds-split-img`). Without that opaque backdrop, the wash's `mix-blend-mode: multiply` crushes the image to black in this rendering path. **Verified the exact fix live via CSS injection before dispatching it** (not guessed) — confirmed the photo renders correctly with the background added. Follow-up dispatched. This is the second real bug this specific lane produced that neither `tsc` nor a self-report would have caught — both only surfaced from actually loading the page in a browser, underlining why the standing "verify in a real browser, not just green build" rule exists.
- 2026-08-28: **All 5 lanes now on staging (`757b16b`), deployed to `development.eternal-fitness.co.uk`, confirmed by Coolify `finished`.** The apparent "still black after the background fix" result from the earlier live-verify was traced to the local dev server / sandboxed browser pane specifically, not the code — confirmed by loading the real deployed build via a real Chrome session (not the automated pane): `/about`'s hero photo renders correctly (Esther, studio background, warm wash visible, no scrim), matching the mockup. Spot-checked `/specialist-training` (split hero renders correctly) and `/faqs` (text-only hero, no image asset, renders as expected) and `/visual-impairment` (completely unaffected — still its original full-bleed overlay hero) — no console errors on any. **CR-EF-086 Lanes 1-5 are all now built, verified, and live on staging.** Production promotion (all 5) is a GATE — queued alongside Lane 1's earlier promotion ask (`qmtd2vl6vgb`), not pushed to `main` without Craig's go-ahead.
- 2026-08-28: **Lane 6 plates delivered by Open Design (`site-image-plates.html`, 31 plates) and hand-reviewed.** Verdicts 17 revise / 10 replace / 4 keep; 27 of 31 carry a described-image disclosure under a reasoned significance test Open Design proposed itself (photograph gets a description when a sighted visitor takes something from it the surrounding words do not carry — room layout, adaptation in action, or largest-image-on-page). 20 of 31 carry a flag. **Not trusted on self-report** — I opened the actual photographs and independently confirmed four of its six headline findings: `studio-1.jpg` is Esther at the kettlebell shelves not an empty studio (the brief's own assumption was wrong); `studio-lunge-pair.jpg` is Esther kneeling with two dogs, no lunge and no client, and it is the full-bleed closing band on `/visual-impairment`; `pricing-studio.jpg`'s live alt says "walking barbell lunge" when neither person holds a barbell; the "BE STRONGER THAN YOUR EXCUSES" wall slogan is legible in 17 of 31 frames including most heroes, which collides with `brand.json` → `imagery.avoid` ("motivational or gym-hype signage visible in frame") and the `voice.vocabulary.avoid` register.
- 2026-08-28: **LIVE DEFECT found via the plates pass, confirmed on production.** `specialist-training-esther-client.jpg` (homepage "Who I work with" section) is stored rotated a quarter turn and carries **no EXIF orientation tag**, so browsers render the rotated pixels as-is. Verified by fetching the raw file from `eternal-fitness.co.uk` and by inspecting the rendered element (landscape-rotated content force-cropped into a 200×351 portrait box, `object-position: 50% 15%`). Nothing else in this WO caught it — not `tsc`, not the Lane 5 alt-text audit, not my own browser verification of Lanes 1–5, because none of those look at what is *in* a photograph. Needs a corrected file, not a copy change; deliberately not fixed here since replacing a live client photograph is Craig/Esther's call.
- 2026-08-29: **Lane 6 unit 1 (GATE) and unit 2 (authoring) CLOSED — Craig approved the plates in Open Design.** Craig flagged that the approval had happened and had not been registered; that was a real gap — the previous session reviewed `site-image-plates.html` and then queued a scope question (`qmtdb2buuem`) instead of treating the approval as the go-ahead, so an approved artifact sat unregistered overnight. Scope answered as delivered: **all 31 plates**, not a phased subset. `wo answer qmtdb2buuem` recorded.
- 2026-08-29: **Plates grounded before registering, not taken on trust.** The artifact lives in the Open Design daemon store (`%APPDATA%/Open Design/.../projects/90556eb1-.../site-image-plates.html`), not the `design-systems` git repo — which is why it was not findable on disk. Extracted its `PLATES` array (31 entries) and ran two checks the previous plate set would have failed: (1) **every one of the 31 filenames exists in `public/images/`** — the 2026-08-25 set referenced 11 photos that were never uploaded; (2) **every one of the 31 maps to a real call site** — 38 sites total via `git grep`, confirming the "same photograph, different alt on different pages" finding. Output committed as `.context/site-image-plates-approved-2026-08-29.json` (build source of truth) + `.context/plate-manifest-approved-2026-08-29.md` (per-plate, with exact call sites and current-alt strings for a precise replace).
- 2026-08-29: **Rotated homepage photo fixed** (Craig chose in-repo rotation). `specialist-training-esther-client.jpg` rotated 90° counter-clockwise, 2400×1600 landscape → 1600×2400 portrait, re-encoded progressive q85 4:2:0 (373KB → 424KB, +14%). Direction verified by looking at the output, not inferred. Framing re-checked by computing the visible source region per the standing rule: at the homepage's 200×351 box, cover now scales to 234×351, so `object-position` Y is a no-op and the crop is a horizontal centre crop losing ~7% each side — both people remain in frame.
- 2026-08-29: **EF-20 held back from the wiring lane.** Open Design was right to mark it "Ask me": with the file rotated it could not read the posture, and its provisional alt ("both with an arm raised out to the side") is **wrong** now the image is upright. The real content is a client stepping up onto a step platform with one knee raised while Esther stands watching by the power rack. Needs re-authoring against the corrected file before it ships — 30 plates wire now, EF-20 follows.
- 2026-08-29: **Standing brand collision confirmed, not just suspected.** With the photo upright the "BE STRONGER TH[AN YOUR EXCUSES]" wall slogan is prominent in the top-right of the frame. That is the `brand.json` → `imagery.avoid` rule ("motivational or gym-hype signage visible in frame") and the `voice.vocabulary.avoid` register, and the plates pass found it legible in 17 of 31 frames including most heroes. This is a photography/reshoot decision for Craig and Esther, not something a wiring lane can fix.
- 2026-08-29: **Lane 6 wiring built, reviewed, fixed, and browser-verified.** OpenCode lane wired 30 of 31 plates (EF-20 held). Diff hand-reviewed line-by-line, and the review caught a **total functional failure that every automated signal missed**: the lane self-reported success, the copy was verbatim-correct, and `tsc` was clean — but it had placed all 27 described-image disclosures *inside* the image wrapper, and every one of those wrappers is either `position: absolute` (`.efhome .hero-media`, `.ds-cta-bg`) or `overflow: hidden` (`.efhome .wimg`, `.ef-tech`, `.ds-cta-split-photo`, `.ds-hero-split-photo`), because `next/image fill` requires a positioned parent. Not one disclosure would have been visible or operable. Second defect: 5 `imageDescription={"...&rsquo;..."}` props put HTML entities inside **braced JSX string literals**, where they are not decoded and render literally (entities only decode in JSX text nodes and plain attributes — my brief's wording caused this). Follow-up lane fixed both.
- 2026-08-29: **Browser-verified at two viewports — the disclosures now render.** Local dev server, all 8 live marketing routes at 1280×900 and the homepage + `/contact` at 375×812: **26 disclosures, 0 not-rendering, 0 clipped, 0 overflowing the viewport, 0 literal entities**, `body.scrollWidth` = 375 on mobile (no horizontal scroll introduced). Interaction tested: opening a disclosure reveals a 555px copy panel. `npx tsc --noEmit` clean. EF-20's original alt confirmed still present; `/visual-impairment` confirmed untouched (0 files changed).
- 2026-08-29: **Own correction, recorded rather than quietly fixed.** An intermediate sweep reported "2 of 26 broken (2px wide)" on `/` and `/testimonials`. That was **wrong** — a measurement artifact: the Browser pane had gone hidden, collapsing `window.innerWidth` to 0, so any element whose width derives from the viewport (the CTA band's `.ds-cta-inner`) measured ~2px while intrinsically-sized ones still measured normally. Re-measured at a forced 1280px viewport: both render correctly at 161px and 1105px. Nothing was ever broken there. Logged because a viewport-dependent false positive is easy to repeat and would have sent a lane chasing a non-existent bug.
- 2026-08-29: **Environment fault found and repaired (was blocking all browser verification).** The shared checkout's `node_modules` had **41 of 95 top-level packages as junctions pointing into deleted worktrees** — `next` itself resolved to a `remove-testimonial-schema` worktree that no longer exists. Fallout from earlier `git worktree remove` cleanups orphaning pnpm store entries that the shared `node_modules` linked into. Every worktree junctions to that directory, so no worktree in this repo could run a dev server or a build. Repaired with `CI=true pnpm install` (14s, no lockfile churn, shared checkout still clean). This also explains the "Cannot find module 'react'/'next/image'" errors the wiring lane saw and correctly dismissed as environmental — after the repair `tsc --noEmit` is genuinely clean.
- 2026-08-29: **Open design question raised, not invented (`qmtdyy...`).** On full-bleed heroes and CTA bands the disclosure renders as a white bar/pill below an edge-to-edge photograph. `/visual-impairment` — the approved quality bar — only ever puts these under *contained, in-content* images inside a `<figure>`, so it does not answer this case. `site-image-plates.html` argues heroes should carry descriptions ("precisely the image that is doing the most work") but specifies no placement or styling for a full-bleed one. Functionally correct and shipped as-is to staging; the visual treatment is a Design Parity question for Craig/Open Design rather than something a lane or I should invent on a client-facing surface.
- 2026-08-29: **Craig rejected the Lane 6 wiring on the deployed staging site — "the describe this image should always sit under the image, they are not." He was right, and the earlier sign-off was wrong.** The verification that passed had checked that the disclosures *rendered* and were not clipped; it never checked they sat **under their own image**. Re-measured properly at 1440×900: **only 4 of 14 were correct.** Two distinct faults: (1) in two-column split heroes/CTA bands the disclosure was an extra grid child, so it became a new grid item in the **first** column — the text column — while the photo sat in the other (on `/about` it was outright inverted: disclosure at x 699–1345, photo at x 80–619); (2) on full-bleed overlay heroes and CTA bands it rendered **over** the image (gaps of −185 and −613) because it was inside the content wrapper rather than below the band.
- 2026-08-29: **Fixed** — contained images: image container + disclosure wrapped in one `<figure class="ef-figure">` that occupies the image's grid slot, so the disclosure inherits the photo's column and width (the shape the 4 already-correct cases used, and the same shape as `/visual-impairment`). Full-bleed backgrounds: disclosure moved outside the `<section>` so it sits below the band.
- 2026-08-29: **That fix introduced a regression, caught before it reached Craig a second time.** The `<figure>` wrapper collapsed the two `next/image` `fill` containers (`.ds-hero-split-photo`, `.ds-cta-split-photo`) to **height 0** — those photographs rendered as a solid black box on `/contact`, `/about` and every split-layout page. Cause: `fill` images are absolutely positioned and contribute no intrinsic height; the containers had been taking their height from the grid row, and the figure removed that. **The positional audit passed it** — it asserted X-alignment and vertical gap but not that the image still had height, so a zero-height photo scored as correct. It was found only by taking a screenshot and seeing a black box where the kettlebells should be. Fixed CSS-only: `.ef-figure > :first-child { flex: 1 1 auto; min-height: 0; }`. **The audit now asserts non-zero image height as well as placement** — that gap is the direct cause of this whole round-trip and is why "it renders" is not the same claim as "it is right".
- 2026-08-29: **Final verification, both assertions together.** Local dev server. Desktop 1440×900, all 8 live marketing routes: **26/26 pass, 0 collapsed figures**. Mobile 375×812 on `/`, `/contact`, `/about`: **14/14 pass, 0 collapsed, no horizontal scroll**. Visually confirmed `/contact` by screenshot — photo renders and the disclosure sits directly beneath it, aligned to the photo's column. `tsc --noEmit` clean. Copy unchanged, EF-20 still untouched, `/visual-impairment` still untouched. Pushed to staging `6eceb22`; Coolify deploy `wj4vw7hn9ynijnn2ff7hqqtk` confirmed in progress.
- 2026-08-29: **Confirmed on the DEPLOYED staging build (not just locally) in Craig's real Chrome session**, since the local-only pass is what let the original fault through. `/contact` split hero: photo renders, disclosure directly beneath it aligned to the photo's column. `/about` in-content split: photo renders, disclosure beneath it, page caption below that. `/` full-bleed hero: disclosure renders as a full-width strip immediately under the hero band. Coolify deploy `wj4vw7hn9ynijnn2ff7hqqtk` completed before checking.
- 2026-08-29: **Correction — a wrong call made and retracted within the same pass.** On first look at deployed `/about` the in-content photos appeared as solid black boxes and were called a resurfacing of the Lane 3 multiply-blend bug. **That was wrong.** They were `next/image` lazy-load placeholders: re-checked with the images given time to load and they render correctly (`complete:false, naturalWidth:0` locally at the same moment confirmed it). No blend regression exists. Logged because calling a load placeholder a rendering bug would have sent a lane chasing nothing.
