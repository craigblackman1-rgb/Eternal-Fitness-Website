# Lane: PWA parity — queue batch: CR-EF-166 move/cancel, CR-EF-167 programme queue view, BUG-EF-134 week label (wo-ef-pwa-deep-dive-2026-09-07)

Repo: eternal-fitness-website. Worktree branch feat/pwa-parity-queue (based on origin/staging — deliberate). Commit per item; do NOT push.

## CR-EF-166 — move/cancel a session from the PWA

Mobile sessions render as plain divs with zero actions (ClientModeView SessionRow ~827, TrainScreen, EditSheet). Desktop already has the full flow: `app/hub/(protected)/clients/[id]/SessionMoveDialog.tsx` (move + three-way cancel charged/free/reschedule with consequence preview) over `PATCH /api/sessions/[id]` (ALLOWED_FIELDS already includes scheduled_at/cancelled_at/cancel_reason/charged_free).

Build a mobile-appropriate bottom-sheet equivalent (match the PWA's existing sheet/dialog patterns — find one in app/hub/m and reuse its structure): Move (date+time picker) and Cancel (three-way: used a session / didn't / reschedule, mirroring desktop's options and consequence copy). Wire it from the session rows in ClientModeView's Sessions pane on upcoming (scheduled, not completed/cancelled) sessions. Reuse desktop's API contract exactly — no new endpoints. Do not add it to TrainScreen (mid-workout is not the place to move a session).

## CR-EF-167 — programme queue on the mobile client screen

`app/hub/m/clients/[id]/page.tsx` never checks `clients.active_program_id`; ClientModeView shows "No active programme" for programme clients and a hand-rolled A–C pool with letters from insertion order (String.fromCharCode(65+poolIndex)).

1. Fetch programme state server-side the way desktop does (`getClientProgramState` from lib/programs/queue.ts — server-only, fine in page.tsx) and pass it to ClientModeView.
2. When a programme exists: render a compact queue strip on the Overview or Pool pane — programme name, week X of Y, next workout (slot label), and the per-slot letters sourced from `program_slots` (mirror desktop `ProgramQueueMap.tsx`'s `slotLetter()` logic — extract it to a shared client-safe helper rather than copying if trivially possible).
3. Pool letters: when the client has a programme, derive letters/labels from slots; keep the current derivation ONLY for programmeless clients.
4. No queue-map grid needed on mobile — a compact strip/list is fine; keep it consistent with the PWA panel language.

## BUG-EF-134 — week label

`app/hub/m/train/[sessionId]/TrainScreen.tsx:1184` renders `Wk {date}` — actually a day/month, labelled as a week. Fix: label scheduled sessions with the date WITHOUT the "Wk" prefix; when the session carries a programme week (sessions.week with program_id present), show "Wk {week}" with the real week number.

## Constraints

- `npx tsc --noEmit` clean; client/server import boundaries (queue.ts server-only; resolve.ts client-safe).
- Three commits, one per item, messages referencing CR-EF-166 / CR-EF-167 / BUG-EF-134, each ending `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- No dev servers/browsers. Match existing PWA visual language; no new design inventions.

Report per item: files, the exact UI entry points added, and API calls made.
