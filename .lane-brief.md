# Lane: PWA parity — display batch: CR-EF-168 PB tile, CR-EF-169 sub-sessions, CR-EF-170 band picker, BUG-EF-136 accordion counts (wo-ef-pwa-deep-dive-2026-09-07)

Repo: eternal-fitness-website. Worktree branch feat/pwa-parity-display (based on origin/staging — deliberate). Commit per item; do NOT push.

## CR-EF-168 — personal-bests summary on the mobile client overview

Desktop computes `exerciseTrendSummary` (personalBests count, heaviest lift, belowBestCount) on `app/hub/(protected)/clients/[id]/page.tsx` (~545-576). Mobile has nothing outside a live session. Add a compact PB tile to ClientModeView's Overview pane (below Medical & compliance): "N personal bests · heaviest X kg" style summary, reusing the same derivation — extract/reuse the desktop computation via a shared lib function rather than duplicating (check lib/ for where buildExerciseTrends/summary logic lives; move only if needed, additively). Data comes from the same set_logs/trainerize sources the desktop page already loads — mirror its fetches in the mobile page.tsx server component.

## CR-EF-169 — supplementary sub-sessions visible on mobile

Sessions with attached sub-sessions (parent_session_id children) are invisible on the phone. Desktop renders+logs them inline (`app/hub/(protected)/clients/[id]/blocks/[blockId]/sessions/[sessionNum]/page.tsx:78-197`). Minimum viable parity per the audit: in TrainScreen, when the session being trained HAS child sub-sessions, list them under the main content as tappable rows (name + exercise count) linking to their own /hub/m/train/[subSessionId] screen (they are sessions rows, so the screen already works for them). Also show a subtle "+N supplementary" marker on session rows in ClientModeView's Sessions pane.

## CR-EF-170 — band picker in the mobile editor

`app/hub/m/train/[sessionId]/edit/EditSheet.tsx` has no band support; desktop fetches `/api/bands` for the CR-EF-014 colour picker (`sessions/[sessionNum]/page.tsx:112-118`). Add the band picker to EditSheet's per-exercise equipment area: fetch the client's band set via the same API the desktop uses, render the colour choices, save to the same field shape desktop writes (find the exercise band field desktop's picker sets — match it exactly so both surfaces read each other's data).

## BUG-EF-136 — accordion counts placeholder flags

`lib/mobile-client-flags.ts` emits placeholder cards like "No conditions recorded". The new accordion (ClientModeView) counts them as items (a Conditions group can read count 2 with one placeholder). Fix: flags whose content is a none-recorded placeholder are excluded from group item counts and from group contents; a group whose only content was placeholders is hidden like an empty group. Identify placeholders by how the lib constructs them (add an explicit `placeholder: true` field at construction rather than string-matching).

## Constraints

- `npx tsc --noEmit` clean; no server-only imports into "use client" files.
- Four commits, one per item, referencing CR-EF-168 / CR-EF-169 / CR-EF-170 / BUG-EF-136, each ending `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- No dev servers/browsers. Match existing PWA visual language; no new design inventions.
- Do NOT touch app/hub/m/clients/[id]/page.tsx's programme-state fetching if you find another lane's changes there mid-run — your base already contains everything you need; if a merge conflict shape appears, stop that item and note it.

Report per item: files, data sources wired, and the field shapes written.
